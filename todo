language switcher
move away from CDN, just in case
literate javascript
be consistent with use of "chapter" vs "section"
move trees throughout to d3, for consistency
remove captions that don't really add anything
add contributors section
add link to bradfield
add analytics tracker
fix rendering on smaller devices

stacks
    fix katex formatting in convert-number-bases.md

trees
    fix bheaps code - seems to fail some tests
    AVL prose needs edeting in light of code changes
    is there a regex to allow for multi-digit numbers in the parse tree example?
    is it a simple fix to allow BST to properly handle duplicate keys?

searching
    move to litpy
    add tests

graphs
    revisit general depth first search to better explain depth first forrests and account for it in the code, for instance using the pancake example
    better example than Luther Computer Science in strongly-connected-components
    for prim's need to fix code to reflect diagrams, add test, interleave diagrams with prose
